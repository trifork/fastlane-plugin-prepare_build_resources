describe Fastlane::Actions::PrepareBuildResourcesAction do
  describe '#run' do
    # it 'prints a message' do
    #   expect(Fastlane::UI).to receive(:message).with("The prepare_build_resources plugin is working!")
    #   Fastlane::Actions::PrepareBuildResourcesAction.run(nil)
    # end
  end
end
